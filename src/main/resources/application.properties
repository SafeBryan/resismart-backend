spring.application.name=ResiSmart Backend
server.port=8080
spring.profiles.active=dev

# --- Conexi�n a Postgres en Docker expuesto en 55432 ---
spring.datasource.url=jdbc:postgresql://localhost:55432/resismart
spring.datasource.username=postgres
spring.datasource.password=200232

# JPA / Hibernate
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# (Opcional) seguridad dev
spring.security.user.name=admin
spring.security.user.password=1234
spring.security.user.roles=ADMIN


spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=resismart.pacasystems@gmail.com
spring.mail.password=ouua joie uozw cleu
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8

# URL del frontend para armar el enlace de restablecimiento
app.frontend.url=http://localhost:4200
# Puedes sobrescribirla directamente si quieres personalizar el path
app.reset-password.base-url=${app.frontend.url}/reset-password

# Mora
resismart.mora.tasaMensual=0.02
